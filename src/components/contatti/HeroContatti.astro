---
---

<section class="relative py-32 md:py-40 overflow-hidden">
  <!-- Background con gradient -->
  <div class="hero-contatti-bg absolute inset-0 bg-gradient-to-br from-caffe-scuro via-caffe-medio to-caffe-scuro"></div>

  <!-- Pattern overlay -->
  <div class="absolute inset-0 opacity-10" style="background-image: radial-gradient(circle at 25% 45%, #D4882C 1px, transparent 1px), radial-gradient(circle at 75% 25%, #D4882C 1px, transparent 1px), radial-gradient(circle at 55% 75%, #D4882C 1.5px, transparent 1.5px); background-size: 60px 60px, 80px 80px, 100px 100px;"></div>

  <!-- Floating shapes -->
  <div class="absolute top-1/3 left-8 w-48 h-48 bg-ambra/5 rounded-full blur-3xl hero-contatti-float"></div>
  <div class="absolute bottom-1/4 right-8 w-56 h-56 bg-crema/5 rounded-full blur-3xl hero-contatti-float-delay"></div>

  <!-- Contenuto -->
  <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div class="reveal-up mb-4">
      <span class="inline-block px-4 py-1.5 text-xs font-semibold uppercase tracking-widest text-ambra border border-ambra/30 rounded-full">
        Contattaci
      </span>
    </div>

    <h1 class="reveal-up font-heading text-4xl sm:text-5xl md:text-6xl font-bold text-crema leading-tight">
      Parliamo del tuo<br>
      <span class="text-ambra">progetto</span>
    </h1>

    <p class="reveal-up mt-6 text-lg sm:text-xl text-crema/75 max-w-2xl mx-auto leading-relaxed">
      Richiedi un sopralluogo gratuito e senza impegno. Ti proporremo la soluzione più adatta ai tuoi spazi e alle tue esigenze.
    </p>
  </div>
</section>

<style>
  @reference "../../styles/global.css";

  .hero-contatti-bg {
    background-size: 200% 200%;
    animation: heroContattiGradient 12s ease infinite;
  }

  @keyframes heroContattiGradient {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .hero-contatti-float {
    animation: heroContattiFloat 8s ease-in-out infinite;
  }

  .hero-contatti-float-delay {
    animation: heroContattiFloat 10s ease-in-out 2s infinite;
  }

  @keyframes heroContattiFloat {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-20px) scale(1.05); }
  }

  @supports (animation-timeline: scroll()) {
    .hero-contatti-bg {
      animation: heroContattiGradient 12s ease infinite, heroContattiParallax linear;
      animation-timeline: auto, scroll();
      animation-range: normal, 0% 50%;
    }

    @keyframes heroContattiParallax {
      0% { transform: translateY(0) scale(1); }
      100% { transform: translateY(10%) scale(1.03); }
    }
  }
</style>